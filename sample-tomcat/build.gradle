plugins {
    id("sample.java-conventions")
    id("application")
}

application {
    mainClass = "sample.SampleTomcatApplication"
}

dependencies {
    implementation(projects.sampleBase)
    implementation(libs.tomcat.embed.core)
}
